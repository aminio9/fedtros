================================================================================
FedTROS-PR Clean Architecture Migration: RL & Legacy Dependency Inventory
================================================================================

1. DIRECTORY: src/rl/
   - environment.py             -> DELETE (RL classification environment with step/reset/reward)
   - replay_buffer.py           -> DELETE (FIFO Experience Replay Buffer)
   - local_training.py          -> REPLACE (Replace RL loop with clean supervised DataLoader epochs)
   - distillation.py            -> REPLACE (Update to deterministic VCT KD & feature alignment)
   - class_balance.py           -> KEEP (Class-balanced cross-entropy & effective number weighting)
   - __init__.py                -> REPLACE (Expose supervised training & distillation utilities)

2. DIRECTORY: src/agents/
   - policy.py                  -> DELETE (EpsilonGreedyPolicy, EpsilonScheduler)
   - agent.py                   -> REPLACE (Remove PriorNet/RecognitionNet/MainQ/TargetQ/GenerationNet/TD-loss;
                                           Add VariationalClassifierTeacher, StudentIDSModel, Aligner)
   - __init__.py                -> REPLACE (Clean exports)

3. DIRECTORY: src/models/
   - models.py                  -> REPLACE (Remove Q-networks, ValueNetwork, GenerationNetwork,
                                           Prior/Recognition nets; Keep ResidualBlock, TabularTransformer)
   - variational_classifier_teacher.py -> NEW (Canonical VCT implementation)
   - student.py                 -> KEEP (Federated StudentIDSModel with 8-D OSR branch)
   - __init__.py                -> REPLACE (Clean exports)

4. DIRECTORY: src/federated/
   - client.py                  -> REPLACE (Remove BlockchainIntrusionEnv, buffer, policy, RL signals;
                                           Use TensorDataset/DataLoader for VCT + Student training)
   - server.py                  -> REPLACE (Ensure FedAvg, FedProx, FedTROS aggregate student parameters)
   - selection_utils.py         -> KEEP / CLEAN (Utility selection functions)

5. DIRECTORY: src/training/
   - centralized.py             -> REPLACE (Supervised centralized training of Student / VCT)
   - smoke.py                   -> REPLACE (Clean supervised smoke run)

6. DIRECTORY: src/checkpointing/
   - checkpoints.py             -> REPLACE (Schema v2 with explicit IncompatibleCheckpointError)

7. DIRECTORY: src/configs/
   - method/fedtros.yaml        -> REPLACE (Clean VCT + student config, purge RL keys)
   - method/fedavg.yaml         -> REPLACE (FedAvg-Student, purge RL keys)
   - method/fedprox.yaml        -> REPLACE (FedProx-Student, purge RL keys)
   - method/fedgpa.yaml         -> REPLACE (Student + prototype alignment)
   - method/centralized_*.yaml  -> REPLACE (Clean centralized configs)
   - training/default.yaml      -> REPLACE (Purge replay_buffer, epsilon, gamma, etc.)
   - experiment/*.yaml          -> REPLACE (Purge generator/RL overrides)
   - utils/config.py            -> REPLACE (Update validation rules)

8. TESTS & CI:
   - tests/test_variational_teacher.py     -> NEW (Unit tests for VCT)
   - tests/test_teacher_student_integration.py -> NEW (KD, alignment, gradient isolation tests)
   - tests/test_rl_removal_guard.py        -> NEW (CI guard forbidding DQN/QNetwork/replay/etc.)
   - tests/test_*.py                       -> REPLACE / UPDATE (Update test suite)

================================================================================

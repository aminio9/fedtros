# @package _global_
experiment:
  method: MSP_Baseline
open_set:
  name: msp
  enabled: true
  evt:
    enabled: false
  scorer:
    name: msp
    requires_fit: false
    score_direction: higher_unknown
    threshold_mode: validation_known_fpr
    threshold_source: validation
    temperature: 1.0

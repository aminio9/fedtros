# @package _global_
experiment:
  id: E7
  name: exp7_efficiency_scalability
  family: efficiency
  method: FMRL_LA
  pipeline: full
  description: Efficiency and scalability over client count and round budget.
  objective: Measure runtime, communication cost, and accuracy per cost.
  primary_metrics:
    - test/accuracy
    - communication/cumulative_mb
    - federated/rounds
  dependencies:
    - preprocessing
    - federated_training
    - closed_set_evaluation
    - suite_artifact_export
  expected_artifacts:
    - communication_metrics.csv
    - federated_history.csv
    - evaluation_metrics.json
federated:
  num_clients: 10
  num_rounds: 100
model:
  num_actions: 5
dataset:
  known_labels: ${dataset.source_labels}
  preprocessing:
    output_dir: ${tracking.run_dir}/processed
    alpha: 0.1
    iid: false
open_set:
  evt:
    enabled: false
training:
  generator:
    enabled: false
evaluation:
  mode: closed_set
  export_latent_embeddings: false
tracking:
  run_id: e7_${experiment.method}_clients${federated.num_clients}_rounds${federated.num_rounds}_seed${seed}

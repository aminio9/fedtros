id: baseline
name: baseline
variant: canonical
pipeline: full
description: BNaT FedTROS-PR baseline with canonical VCT training and Prototype-Rank evaluation.
method: FedTROS-PR
dataset: BNaT
partition: dirichlet
alpha: ${dataset.preprocessing.alpha}
notes: "Use saved metrics/CSV artifacts for every figure; do not use template arrays as evidence."

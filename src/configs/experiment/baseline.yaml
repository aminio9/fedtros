id: baseline
name: baseline
variant: canonical
pipeline: full
description: BNaT FedTROS-MC baseline with canonical VCT training and Multicenter Conformal evaluation.
method: FedTROS-MC
dataset: BNaT
partition: dirichlet
alpha: ${dataset.preprocessing.alpha}
notes: "Use saved metrics/CSV artifacts for every figure; do not use template arrays as evidence."

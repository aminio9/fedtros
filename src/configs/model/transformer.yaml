name: tabular_transformer
feature_dim: 31
latent_dim: 64
hidden_dim: 256
projection_dim: 128
dueling_hidden_dim: 128
activation: gelu
dropout: 0.1
use_generation_network: true
generation_hidden_dim: 64
transformer:
  enabled: true
  input_dim: ${model.feature_dim}
  d_model: 128
  nhead: 4
  num_layers: 2
  dim_feedforward: 256
  dropout: 0.1
  pooling: mean

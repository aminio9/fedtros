name: fedtros
feature_dim: 31
num_classes: 4
latent_dim: 64
hidden_dim: 512
projection_dim: 256
activation: gelu
dropout: 0.05

# Compatibility aliases
input_dim: ${model.feature_dim}

transformer:
  enabled: false
  input_dim: ${model.feature_dim}
  d_model: 128
  nhead: 4
  num_layers: 2
  dim_feedforward: 256
  dropout: 0.1
  pooling: mean

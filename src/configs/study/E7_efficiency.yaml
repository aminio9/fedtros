study_id: E7-EFFICIENCY
name: E7_efficiency
description: Communication and computational efficiency using architecture-matched student baselines and actual payload bytes.
methods: [fedtros_pr, fedavg, fedprox]
datasets: [bnat]
alphas: [0.5]
iids: [false]
unknown_label_sets: [[FoT]]
known_labels: [Normal, BP, DoS, MitM]
seeds: [17, 42, 73, 101, 137]
num_clients: 10
base_overrides:
  federated.num_rounds: 100
  training.local_epochs: 2
  evaluation.mode: open_set

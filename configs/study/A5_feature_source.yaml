# A5 Feature Source Selection Ablation
study_id: A5-FEATURE-SOURCE
name: A5_feature_source
description: Ablation comparing open-set rejection features from student latent bottleneck vs teacher representations.

methods:
  - fedtros_pr
datasets:
  - bnat
alphas:
  - 0.1
iids:
  - false
unknown_label_sets:
  - ["FoT"]
seeds:
  - 17
  - 42
  - 73
num_clients: 10

base_overrides:
  federated.num_rounds: 50
  training.local_epochs: 1
  evaluation.mode: open_set

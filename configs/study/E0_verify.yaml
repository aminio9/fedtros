study_id: E0-VERIFY
name: E0_verify
description: Protocol and implementation verification gate; no publication claims.
methods: [fedtros_mc]
datasets: [bnat]
alphas: [0.5]
iids: [false]
unknown_label_sets: [[FoT]]
known_labels: [Normal, BP, DoS, MitM]
seeds: [42]
num_clients: 2
base_overrides:
  federated.num_rounds: 2
  training.local_epochs: 1
  evaluation.mode: open_set

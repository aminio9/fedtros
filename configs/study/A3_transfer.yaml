# A3 Knowledge Transfer Schedule Ablation
study_id: A3-TRANSFER
name: A3_transfer
description: Ablation of teacher-to-student distillation onset rounds and latent alignment schedules.

methods:
  - fedtros_pr
datasets:
  - bnat
alphas:
  - 0.1
iids:
  - false
unknown_label_sets:
  - ["FoT"]
seeds:
  - 17
  - 42
  - 73
num_clients: 10

base_overrides:
  federated.num_rounds: 50
  training.local_epochs: 1
  evaluation.mode: open_set
